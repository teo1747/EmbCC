# src/cpp

Preprocessor — ../../docs/ARCHITECTURE.md §2 and §5.

M0 state: only the predefined-macro table (predef.c, generated by
../../tools/gen-predef.sh from `x86_64-elf-gcc -dM -E` — never edited by
hand). The preprocessor itself is M2 work.
